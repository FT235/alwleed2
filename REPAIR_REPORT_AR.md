# تقرير إصلاح مشروع مؤسسة الوليد للإنسانية

## الخلاصة

تم إصلاح المشروع محليًا وتسليمه في بنية جذر صحيحة. أصبح `package.json` في الجذر مباشرة، وأصبح الخادم يعتمد على متغيرات البيئة بدل بيانات الدخول الثابتة. لم يتم النشر على Railway، ولم تُستخدم أي حسابات أو رموز وصول، ولم تُعدّل سجلات DNS أو تُحذف بيانات قاعدة SQLite الأصلية.

## التعارض الذي تم العثور عليه

كان ملف الاختبار القديم يتوقع تسجيل الدخول بالبيانات التالية:

- البريد: `admin@alwaleed.local`
- كلمة المرور: `ChangeMe!2026`

أما سجل المشرف الموجود فعليًا في SQLite فكان:

- البريد: `fthyhsyn0712@gmail.com`
- الدور: `admin`
- المعرّف: `2`
- كلمة المرور مخزنة كقيمة bcrypt مجزأة، لذلك لا يمكن استعادة كلمة المرور الأصلية من قاعدة البيانات.

كما كان README القديم يعرض بريدًا وكلمة مرور قديمين بصورة صريحة. تم حذف هذا الكشف من README.

## الإصلاحات المنفذة

1. نقل محتويات المشروع من المجلد الفرعي إلى الجذر بحيث يوجد `package.json` بجانب `server.mjs`.
2. تعديل الخادم ليقرأ `ADMIN_EMAIL` و`ADMIN_PASSWORD` من البيئة.
3. إضافة منطق بدء التشغيل الذي ينشئ المشرف إذا لم يوجد، أو يحدّث المشرف الموجود ليطابق البيئة مع إبقاء دوره `admin`.
4. استخدام bcrypt لتجزئة كلمة المرور الجديدة.
5. استخدام `DATABASE_PATH` بدل المسار الثابت.
6. إنشاء مجلد قاعدة البيانات تلقائيًا.
7. نسخ `.data/alwaleed.sqlite` إلى `/data/alwaleed.sqlite` عند أول تشغيل على Railway إذا لم يوجد الملف الدائم.
8. إضافة حفظ قاعدة البيانات بعد عمليات الإضافة والتعديل والحذف.
9. إضافة `.env.example` و`.gitignore` و`railway.json`.
10. تحديث الاختبار ليستخدم بيانات البيئة بدل بيانات قديمة ثابتة.
11. إضافة تحميل تلقائي لملف `.env` عند التشغيل المحلي من دون كشفه في ZIP.
12. استبدال README بتعليمات آمنة وإضافة دليل نشر عربي كامل.

## بيانات البيئة

أنشأ الإصلاح ملف `.env` محليًا يحتوي على `JWT_SECRET` عشوائي طوله 64 حرفًا، وكلمة مرور إدارية عشوائية قوية. الملف غير موجود في ZIP النهائي، ولا ينبغي رفعه إلى GitHub. البريد الإداري المقترح هو:

`admin@alwaleedcharitablefoundation.com`

يوجد نموذج غير سري في `.env.example` يوضح أسماء المتغيرات المطلوبة. عند النشر على Railway يجب إدخال القيم يدويًا في تبويب Variables.

## الملفات التي عُدّلت أو أُنشئت

- `server.mjs`
- `server.test.mjs`
- `README.md`
- `.env.example`
- `.gitignore`
- `railway.json`
- `DEPLOYMENT_GUIDE_AR.md`
- `REPAIR_REPORT_AR.md`

## نتيجة الاختبار المحلي

| الاختبار | النتيجة |
|---|---|
| فحص صياغة `server.mjs` | ناجح |
| فحص صياغة `server.test.mjs` | ناجح |
| `npm test` | حالتا اختبار ناجحتان |
| الصفحة الرئيسية `/` | HTTP 200 |
| لوحة الإدارة `/admin` | HTTP 200 |
| واجهة المحتوى `/api/content` | HTTP 200 |
| إنشاء طلب جديد | ناجح وأعاد رقمًا يبدأ بـ `ALW-` |
| API التتبع | ناجح وأعاد الحالة `قيد المراجعة` |
| تسجيل الدخول بالحساب الجديد | ناجح وأعاد JWT ودور `admin` |
| قراءة قائمة الطلبات | ناجحة بعد التحقق بالتوكن |

## محتويات ZIP النهائي

تم استبعاد العناصر التالية من ZIP كما هو مطلوب:

- `node_modules`
- `.env`
- ملفات `*.sqlite`
- الملفات المؤقتة والنسخ الاحتياطية الداخلية

لخطوات الرفع إلى GitHub والنشر على Railway وإضافة Volume وربط النطاق، راجع `DEPLOYMENT_GUIDE_AR.md`.
