# دليل تشغيل ونشر مشروع مؤسسة الوليد للإنسانية

## النتيجة المختصرة

تم إصلاح المشروع وتجهيزه ليعمل محليًا على Node.js وExpress وSQLite، وليُرفع لاحقًا إلى Railway. **لم يتم تنفيذ أي نشر خارجي، ولم تُستخدم أي حسابات أو رموز وصول، ولم تُعدّل سجلات DNS.** قاعدة البيانات الأصلية بقيت موجودة ولم تُحذف منها البيانات.

## بيانات المشرف بعد الإصلاح

أصبح الخادم يقرأ البريد وكلمة المرور من متغيري البيئة `ADMIN_EMAIL` و`ADMIN_PASSWORD`. عند بدء التشغيل، إذا لم يوجد مشرف فسيُنشأ مشرف جديد. وإذا كان هناك مشرف، فسيُحدَّث البريد وكلمة المرور لذلك الحساب مع إبقاء رقمه وبياناته الأخرى. الحساب يحصل دائمًا على الدور `admin`.

البريد المقترح للاستخدام هو `admin@alwaleedcharitablefoundation.com`. كلمة المرور القوية موجودة في ملف `.env` المحلي، ولا تُرفع إلى GitHub ولا تُرسل إلى أي شخص. غيّرها إذا شاركت الجهاز مع شخص آخر.

## 1. رفع المشروع إلى GitHub من الهاتف

1. نزّل ملف `alwaleed-project-fixed.zip` وفك ضغطه على الهاتف.
2. تأكد أن الملفات مثل `package.json` و`server.mjs` موجودة في المجلد الرئيسي مباشرة، وليس داخل مجلد إضافي.
3. أنشئ مستودعًا جديدًا في [GitHub](https://github.com/). المستودع هو مساحة تخزين المشروع على الإنترنت.
4. ارفع محتويات المجلد إلى المستودع باستخدام **ZipToGit** أو أي أداة رفع ملفات إلى GitHub. لا ترفع ملف ZIP نفسه إذا كانت الأداة لا تفك الضغط تلقائيًا.
5. قبل الضغط على الرفع، تأكد من عدم وجود الملفات التالية:
   - `.env`؛ لأنه يحتوي أسرار الدخول.
   - `node_modules`؛ لأنه مجلد كبير ينشئه Railway تلقائيًا.
   - أي ملف ينتهي بـ `.sqlite`؛ لأن النسخة الأصلية لا ينبغي نشرها داخل GitHub.
6. اجعل المستودع خاصًا إن لم تكن بحاجة إلى نشر الكود للعامة.

## 2. إنشاء حساب Railway

1. افتح [Railway](https://railway.com/).
2. اختر التسجيل أو تسجيل الدخول عبر GitHub.
3. وافق على ربط Railway بحساب GitHub عند ظهور شاشة الصلاحيات.
4. قد يطلب Railway تحققًا إضافيًا من الهوية أو وسيلة دفع حسب حالة الحساب وسياسة الخدمة. لا تضف وسيلة دفع إلا إذا قررت ذلك بنفسك.

## 3. إنشاء مشروع جديد

1. من لوحة Railway اختر **New Project**.
2. اختر **Deploy from GitHub repo**.
3. اختر مستودع المشروع.
4. انتظر انتهاء البناء. سيقرأ Railway ملف `package.json` ويشغّل أمر `npm start` الموجود في `railway.json`.
5. افتح سجلات البناء. إذا ظهر أن البناء نجح، انتقل إلى إعدادات الخدمة.

## 4. إضافة قرص دائم لقاعدة البيانات

قاعدة SQLite ملف واحد. القرص الدائم، أو **Volume**، هو مساحة لا تُمسح عند إعادة نشر الخدمة.

1. افتح خدمة المشروع في Railway.
2. افتح تبويب **Volumes** أو إعدادات التخزين.
3. اختر إضافة قرص دائم جديد.
4. اجعل **Mount path** يساوي `/data` حرفيًا.
5. احفظ الإعداد ثم أعد النشر إذا طلب Railway ذلك.

> **تحذير مهم:** إذا شغّلت المشروع من دون Volume، فقد تختفي الطلبات والرسائل عند إعادة النشر أو إعادة تشغيل الخدمة.

## 5. إضافة متغيرات البيئة

افتح تبويب **Variables** في خدمة Railway وأضف المتغيرات التالية:

| الاسم | القيمة المطلوبة |
|---|---|
| `NODE_ENV` | `production` |
| `PORT` | اتركه فارغًا إذا كان Railway يحقنه تلقائيًا، أو استخدم قيمة Railway الموصى بها |
| `JWT_SECRET` | قيمة عشوائية طويلة لا تقل عن 32 حرفًا |
| `ADMIN_EMAIL` | `admin@alwaleedcharitablefoundation.com` أو بريدك الإداري الحقيقي |
| `ADMIN_PASSWORD` | كلمة مرور قوية مختلفة عن أي كلمة أخرى |
| `DATABASE_PATH` | `/data/alwaleed.sqlite` |

لا تضع هذه القيم داخل ملفات JavaScript. لا تنشر `JWT_SECRET` أو `ADMIN_PASSWORD` في GitHub أو في صور الشاشة أو في رسائل عامة. إذا انكشفت أي قيمة، غيّرها فورًا من Railway.

عند أول تشغيل مع `DATABASE_PATH=/data/alwaleed.sqlite`، ينسخ المشروع قاعدة `.data/alwaleed.sqlite` الموجودة في الحزمة إلى `/data` إذا لم يكن الملف الدائم موجودًا بعد. بعد ذلك يستخدم الملف الموجود على القرص الدائم.

## 6. النشر والمعاينة

1. بعد حفظ المتغيرات، اضغط **Deploy** أو **Redeploy**.
2. افتح **Settings** ثم أنشئ نطاق Railway العام من خيار **Generate Domain** أو الخيار الذي يحمل معنى مشابهًا.
3. افتح الرابط العام واختبر الصفحات التالية:
   - الصفحة الرئيسية: `/`
   - لوحة الإدارة: `/admin`
   - صفحة التقديم: `/apply`
   - التتبع: `/track`
4. سجّل الدخول من لوحة الإدارة باستخدام `ADMIN_EMAIL` و`ADMIN_PASSWORD` اللذين أدخلتهما في Railway.
5. أنشئ طلبًا تجريبيًا، ثم افتح رابط التتبع الذي يعيده النظام.

## 7. اختبار ثبات قاعدة البيانات

1. أنشئ طلبًا جديدًا من صفحة التقديم.
2. احتفظ برقم الطلب الذي يبدأ غالبًا بـ `ALW-`.
3. نفّذ **Redeploy** من Railway.
4. بعد عودة الخدمة، افتح واجهة الإدارة وابحث عن الطلب.
5. افتح صفحة التتبع واكتب رقم الطلب.
6. إذا بقي الطلب موجودًا بعد إعادة النشر، فهذا يعني أن Volume ومسار `/data` يعملان كما يجب.

## 8. ربط النطاق `alwaleedcharitablefoundatio.com`

لا تنفّذ هذه الخطوة إلا بعد نجاح الرابط العام في Railway.

1. في Railway افتح إعدادات الخدمة واختر **Custom Domain** أو **Domains**.
2. أضف النطاق `alwaleedcharitablefoundatio.com`.
3. سيعرض Railway سجل DNS المطلوب. قد يكون سجل `CNAME` أو سجلًا آخر بحسب إعداد الحساب؛ استخدم القيمة التي يعرضها Railway، ولا تخمّنها.
4. سجّل الدخول إلى [Namecheap](https://www.namecheap.com/) وافتح **Domain List** ثم **Manage** بجوار النطاق.
5. افتح **Advanced DNS**.
6. أضف السجل الذي أعطاك Railway. عادةً يكون المضيف `@` للنطاق الأساسي، أو `www` للنطاق الفرعي، لكن اتبع تعليمات Railway الحالية بدقة.
7. لا تحذف سجلات البريد أو السجلات الحالية قبل قراءتها. تغيير DNS بشكل خاطئ قد يوقف الموقع أو البريد الإلكتروني.
8. احفظ التغييرات وانتظر انتشار DNS. قد يستغرق ذلك بعض الوقت.
9. عُد إلى Railway وانتظر تأكيد النطاق وشهادة HTTPS، ثم اختبر النطاق في متصفح خاص.

راجع تعليمات [Railway الرسمية](https://docs.railway.com/) وتعليمات [Namecheap الرسمية لإضافة Host Records](https://www.namecheap.com/support/knowledgebase/article.aspx/434/2237/how-do-i-set-up-a-host-record-for-my-domain/) إذا تغيرت أسماء الأزرار.

## 9. بعد نجاح النشر

احذف أي رمز وصول مؤقت أنشأته بنفسك. احتفظ بنسخة احتياطية دورية من ملف SQLite الموجود على القرص الدائم. لا تحفظ النسخة الاحتياطية في مستودع GitHub عام. اختبر تسجيل الدخول والتقديم والتتبع بعد كل تغيير كبير.

## الملفات المهمة في النسخة المصححة

| الملف | الغرض |
|---|---|
| `server.mjs` | الخادم والمسارات وتهيئة قاعدة البيانات والمشرف |
| `.env.example` | أسماء متغيرات البيئة المطلوبة دون أسرار |
| `railway.json` | إعداد البناء والتشغيل وفحص الصحة في Railway |
| `.gitignore` | منع رفع الأسرار والاعتماديات وقواعد SQLite |
| `server.test.mjs` | اختبار تسجيل الدخول والتقديم والتتبع |
| `DEPLOYMENT_GUIDE_AR.md` | هذا الدليل |

## مراجع

[1]: https://docs.railway.com/ "Railway Documentation"
[2]: https://www.namecheap.com/support/knowledgebase/article.aspx/434/2237/how-do-i-set-up-a-host-record-for-my-domain/ "Namecheap: How do I set up a Host Record?"
[3]: https://github.com/ "GitHub"
[4]: https://railway.com/ "Railway"
